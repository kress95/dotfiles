#!/bin/env sh

main=$(xrandr -q | grep -m 1 " connected" | cut -d" " -f1)

if [ $main = "HDMI-1" ]; then
    # yay, hatin' synergy
    ssh -i ~/.ssh/kress_arch_id_rsa.pubkress-arch -X "DISPLAY=:0.0 xdotool key ctrl+alt+j"
else
    xdotool key ctrl+alt+u
fi